export class MarvelHero {
  name: string;
  commics: number;
  series: number;
  tumb: string

  constructor(name, commics, series, tumb) {
    this.name = name;
    this.commics  = commics;
    this.series = series;
    this.tumb = tumb;
  }
}
